<!DOCTYPE html>
<html>
<head>
    <title>Create a New Post</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <!-- New Post Form -->
    <h1 class="header">Create a New Post</h1>
    <form action="/posts" method = "POST" class="section">
        <label for="title" class="post_label"> Title: </label>
        <input type="text" id = "title" name = "title" required>

        <label for="author" class="post_label">Author:</label>
        <input type="text" id="author" name="author" required>

        <label for="content" class="post_label"> Content: </label>
        <textarea name="content" id="content" required></textarea>

        <label for="category" class="post_label">Category:</label>
        <select name="category" id="category" required>
            <% categories.forEach(category => { %>
                <option value="<%= category._id %>"><%= category.categoryName %></option>
            <% }); %>
        </select>

        <label for="tags" class="post_label">Tags (comma-separated):</label>
        <input type="text" id="tags" name="tags">

        <button type="submit" class="button"> Publish </button><br>
        <a href="/" >Back to Home</a>
    </form><br>
</body>
</html>